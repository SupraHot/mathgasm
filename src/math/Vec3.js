import { __Native } from '../util/Bootstrapper.js'

class Vec3 {
    constructor( x, y, z ) {
        this.x = x;
        this.y = y;
        this.z = z;
    }

    add( v ) {

        

        // Wrap mathgasm

    }
}

export { Vec3 };